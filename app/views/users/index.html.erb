<h2> List of users </h2>

<ul>
  <% @users_array.each do |the_user| %>
    <li>
      <a href="<%= user_path(the_user) %>">
        <%= the_user.name %> -
        <%= the_user.email %>
      </a>
    </li>
  <% end %>
</ul>
